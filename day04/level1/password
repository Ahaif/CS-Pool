password : "__stack_check"

// i crack it using ltrace ./level1 => shows function calls to external library
// a call to string comapre to authenticate user password


// bypassing authentification by using gdb
    //run gdb on binary file and set break point before the jump call that grant access
    // set $eax=0 return of strmcp , 
    //you can grant acess with any password typed since it change the value of register in execution line